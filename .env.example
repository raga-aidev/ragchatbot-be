# GCP Configuration
# Your Google Cloud Project ID - REQUIRED
GCP_PROJECT_ID=your-gcp-project-id

# Vertex AI Configuration
# Region where Vertex AI is deployed
GCP_VERTEX_AI_LOCATION=us-central1

# Vertex AI model to use for SQL generation
GCP_VERTEX_AI_MODEL=gemini-2.5-pro

# BigQuery Configuration
# Public dataset - not sensitive
GCP_BIGQUERY_DATASET=bigquery-public-data
GCP_BIGQUERY_SCHEMA=ncaa_basketball

# CORS Configuration
# Comma-separated list of allowed origins for frontend
CORS_ALLOWED_ORIGINS=http://localhost:4200

# Instructions:
# 1. Copy this file to .env in the same directory
# 2. Replace 'your-gcp-project-id' with your actual GCP project ID
# 3. Adjust other values as needed for your environment
# 4. Never commit the .env file to version control
